import { BrowserRouter } from "react-router-dom";
import Router from "./routes";

export const RoutesManager = ({}) => (
  <BrowserRouter>
    <div className="App0">
      <Router />
    </div>
  </BrowserRouter>
);

